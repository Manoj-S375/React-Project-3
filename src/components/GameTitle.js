import React from "react";

const GameTitle = () => {
    return <div className="game-title">
        <h1 className="GTitle">Connect - 4</h1>
        <div className="GText">Connect 4 tiles either horizontally,vertically or diagonally</div>
    </div>;
};

export default GameTitle;
